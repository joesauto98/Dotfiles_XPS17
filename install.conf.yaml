- defaults:
      link:
          relink: true

- clean: ['~']

- link:
      #~/.vim: vim
      ~/.zshrc:
      ~/.gitconfig:

- create:
      - ~/Work

- shell:
      - command: ./setup_script.zsh #THIS IS THE INSTALL SCRIPT COMMAND TO TO SEARCH YAY FOR PACKAGES
        stdout: true
        stderr: true
